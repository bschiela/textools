\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{wfs-utils}[2019/04/02]

\RequirePackage[x11names]{xcolor}

\RequirePackage{doi}
\RequirePackage{hyperref}
\hypersetup{
	colorlinks=true,
	% internal links
	linkcolor=Green4,
	citecolor=Green4,
	% external links
	urlcolor=Blue4
}

\RequirePackage{graphicx}
\graphicspath{{./res/graphics/}}
\RequirePackage{media9} % flash media
\addmediapath{./res/media/}

\RequirePackage{framed}

% comment.sty comment environment clashes with changes.sty \comment macro
\@ifpackageloaded{changes}{
	\let\wfs@changes@comment\comment
	\let\comment\@undefined
	
	\RequirePackage{comment}
	\let\wfs@comment@comment\comment
	\let\comment\@undefined
	
	\newcommand\comment{%
		\ifthenelse{\equal{\@currenvir}{comment}}
			{\wfs@comment@comment}
			{\wfs@changes@comment}%
	}
}{\RequirePackage{comment}}

% page skipping
\RequirePackage{etoolbox}
\newcommand*\skippage[1][\@nil]{
	\newpage\null%
	\ifblank{#1}{}{\thispagestyle{#1}}%
	\newpage%
}
